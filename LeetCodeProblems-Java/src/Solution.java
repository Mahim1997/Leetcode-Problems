class Solution {
    // for each row, <start, end>

    public int maximalRectangle(char[][] matrix) {
        Utils.printMatrixCharacter(matrix);

        return 0;
    }
}
class Position{
    int row;
    int col;
}